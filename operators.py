operand1 = 250.66
operand2 = 1008.2

print("{:.2f} + {:.1f} = {:.2f}".format(operand1, operand2, operand1 + operand2))
print("{:.2f} - {:.1f} = {:.2f}".format(operand1, operand2, operand1 - operand2))
print("{:.2f} * {:.1f} = {:.2f}".format(operand1, operand2, operand1 * operand2))
print("{:.2f} / {:.1f} = {:.2f}".format(operand1, operand2, operand1 / operand2))
print("{:.2f} % {:.1f} = {:.2f}".format(operand1, operand2, operand1 % operand2))

